export type DespesasType = {
  dpcodigo: string;
  dpvalor: number;
  dpdescricao: string;
  dpcategoria: string;
  dpveiculo: string;
  dpquando: string;
  dpkm: number;
  createdAt: string;
  updatedAt: string;
};
